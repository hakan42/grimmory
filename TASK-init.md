Read current code base. Understand how to add new metadata plugins.

For TASK files, keep to the conventions in sibling projects, but do not touch gitingore files

write an AGENTS.md, and CLAUDE.md referencing (not a softlink), but do not touch README. Those files are not to be committed.
